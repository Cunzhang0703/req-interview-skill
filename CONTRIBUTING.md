# 贡献指南

感谢愿意改进 `req`。在动手之前，请先读完这一页——这个仓库有一条不太一样的规则。

## 最重要的一条：`SKILL.md` 不是散文，是代码

`SKILL.md` 里每一个词都在塑造 Agent 的行为。删掉一个"不能自问自答"，技能就可能开始自说自话；
把"唯一指定"改成"建议使用"，弹窗提问就会退化成正文列 A/B/C。

所以：**任何改变行为的措辞改动，都必须附上改动前后的实测对比。**

什么算证据：

- 在同一宿主、同一模型下，用同一个测试场景分别跑**改动前**和**改动后**两个版本；
- 贴出两边实际行为的差异，说明它为什么是改进；
- 至少覆盖一个**对抗场景**（用户说"随便"、用户一次答不完、用户中途改口之类），不能只跑顺风路径。

"改完看起来更清楚了"不是证据。没有证据的行为类 PR 会被挂起。

## 双版本同步规则

`skills/req-workbuddy/SKILL.md` 与 `skills/req-openai/SKILL.md` 是**同一套规则的两种宿主表述**。

除下面这四处**刻意保留的差异**外，两份文件必须逐字一致：

| 位置 | `req-workbuddy` | `req-openai` |
| --- | --- | --- |
| frontmatter `description` | 写明"通过 WorkBuddy 原生选择弹窗分轮追问" | 写为"通过分轮追问"（宿主无关） |
| §3 小节标题与正文 | 「提问一律用 AskUserQuestion」，工具唯一指定 | 「提问优先用宿主的原生提问工具」，无工具时降级 |
| §8 确认方式 | 明确用 `AskUserQuestion` 收集范围反馈 | 写为"宿主的结构化提问工具（没有则普通提问）" |
| §9 模式切换 | 「由用户在 WorkBuddy 里自行切换」 | 「由用户自行在宿主中切换」 |

改一处，就要检查另一处。提 PR 前用这条命令自查，输出应为空或只命中上表四处：

```bash
diff skills/req-workbuddy/SKILL.md skills/req-openai/SKILL.md
```

`assets/icon.svg` 两版必须完全相同（当前 MD5 一致）。改动图标要同时更新两处。

## 一次 PR 只做一件事

混装多个不相关改动的 PR 会被要求拆分。把"修错别字"和"调整追问节奏"分开提。

## 不要提交个人化内容

- 不要在 `SKILL.md` 里写死你自己的称呼、口头禅或项目路径——技能面向所有人。
- 个人称呼应当在使用时于对话中提出，而不是写进共享技能。
- 不要引入任何需要联网、需要付费服务或需要额外安装依赖才能工作的内容。

## 提交信息

用 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/) 风格，中文描述即可：

```text
fix: 修正 §3 中"暂不确定"选项的措辞
feat(openai): 补充隐式调用关闭时的降级说明
docs: 补安装到项目级目录的示例
chore: 同步两版 icon.svg
```

## 提 Issue

用仓库自带的模板（[Bug 报告](../../issues/new?template=bug_report.md) / [功能建议](../../issues/new?template=feature_request.md)）。
提之前先搜一遍已有和已关闭的 Issue。

描述问题时请带上：**宿主 + 版本、模型、你实际输入的原文、Agent 实际的反应、你期望的反应**。
"它不好用"没法排查；一段真实对话记录是最有价值的东西。

## 流程

1. Fork 或新建分支（不要直接往 `main` 推）；
2. 按上面的规则改，跑完自查；
3. 一次一件事，提 PR 并填写模板；
4. 在 `CHANGELOG.md` 的 `[未发布]` 下补一条记录。

## 验收场景（改完必须各跑一遍）

在两个平台分别用下面的场景验收，别用"文件格式正常"代替实际效果验证：

1. **全新模糊需求**——"我想做个记账工具"。期望：先只读调查 + 复述理解，问 1—3 个关键问题，
   **不**直接给技术方案。
2. **明确小改动**——"把首页那个按钮改成蓝色"。期望：走完整访谈即为失败。
3. **含糊授权**——用户对"数据是否公开"回答"随便"。期望：保持阻塞或明确列为暂不做，
   **不**自行判定为已授权。
4. **图示交付**——复杂需求。期望：对话内有可渲染的 Mermaid，SVG 按 §6 保存或如实说明未保存。
5. **确认口径**——用户回"继续"。期望：不当作需求已确认。
