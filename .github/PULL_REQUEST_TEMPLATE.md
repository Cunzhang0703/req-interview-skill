<!--
提交前请读完这份模板。留空必填项、混装多个不相关改动、或没有任何实测证据的行为类 PR，
会被要求补充后再审。
-->

> **本 PR 请指向 `main` 以外的分支。** `main` 是对外发布分支，请从 `dev` 或你自己的分支发起。

## 这个 PR 在解决什么问题

<!-- 描述你实际遇到的问题。请带上场景：你在做什么、Agent 做错了什么。
     "优化一下措辞"不是问题描述——哪里不对？会导致什么后果？ -->

## 改了什么

<!-- 1—3 句，只说改了什么，原因写在上一节。 -->

## 改动类型

- [ ] **行为改动**——改了 `SKILL.md` 的措辞、限制或流程（**必须附实测证据**）
- [ ] 文档 / 结构改动——只动 README、docs、模板等
- [ ] 资源改动——只动图标、`openai.yaml` 等

## 改动属于哪一节

<!-- 例如：§3 分轮追问 / §6 需求可视化。方便审阅者定位。 -->

- 节号：§

## 行为改动的实测证据（行为类必填）

本项目把 `SKILL.md` 当作代码看：改一个词就可能改变 Agent 行为。请填下表。

| 项目 | 内容 |
| --- | --- |
| 宿主 + 版本 | |
| 模型 + 版本 | |
| 测试场景（原文输入） | |

| 场景 | 改动前行为 | 改动后行为 | 为什么是改进 |
| --- | --- | --- | --- |
| 顺风路径 |  |  |  |
| 对抗场景（用户说"随便"／中途改口／一次答不完等） |  |  |  |

<!-- "改完看起来更清楚了"不算证据。没有实测对比的行为类 PR 会被挂起。 -->

## 双版本同步

- [ ] 我确认两版差异**仅限** CONTRIBUTING 里列出的四处，并已运行 `diff` 自查
- [ ] 本次改动只影响单一宿主（请说明理由）

<details>
<summary>diff 自查输出</summary>

```text
（粘贴 diff skills/req-workbuddy/SKILL.md skills/req-openai/SKILL.md 的结果）
```

</details>

## 是否只做了一件事

- [ ] 是，本 PR 只包含一个逻辑改动
- [ ] 否（请拆分成多个 PR；确实有依赖关系的请说明）

## 是否引入依赖

- [ ] 否，仍是纯提示词、无脚本、无网络请求
- [ ] 是（请说明；这类改动通常不会被接受）

## 提交前检查

- [ ] 已在 `CHANGELOG.md` 的 `[未发布]` 下补了记录
- [ ] 未在 `SKILL.md` 中写入个人称呼、口头禅或本机路径
- [ ] 已按 CONTRIBUTING 的验收场景实跑

<!--
以下情况会被直接关闭：
- 展示不出任何人工参与痕迹
- 混装多个不相关改动
- 引入第三方服务或工具
- 把个人配置当成通用规则提交
- 必填项留空或仍为占位文本
- 行为类改动没有 eval 证据
-->
